export const getPasswordRecoveryPage = async (req: any, res: any) => {
    res.render("Password recovery");
}

export const postPasswordRecoveryPage = async (req: any, res: any) => {
    res.render("Password recovery");
}